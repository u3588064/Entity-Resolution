
# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema:

  commandFunction:
    # A function that produces the CLI command to start the MCP on stdio.
    |-
    (config) => ({
    "command": "node",
    "args": [
      "build/index.js"
      ],
      "env": {
       
      }
    })
